# 项目问题与想法

# **问题**

## 1. 面试实时 Session 仅保存在 Node.js 内存中

### 现状

面试实时 Session 使用 Node.js 进程内的 `Map` 保存，并以 `userId` 作为键。

### 影响

- 后端重启、崩溃或重新部署后，内存会清空；正在进行的面试流程状态会丢失。
- 同一个用户只保留一份 Session；如果用户同时在两个标签页或两个设备开始面试，后一次会覆盖或复用前一次状态。
- 当后端运行多个实例时，各实例有各自独立的内存。请求被分配到不同实例后，后一个实例无法读取前一个实例的 Session，导致面试上下文、提问计划和进度不一致。

### 改进想法

将 Session 改为以 `interviewId` 为键，并存入 Redis 或 Supabase/PostgreSQL：

- Redis：适合频繁读写的实时状态，并可设置过期时间。
- PostgreSQL：适合需要可恢复、可审计的面试状态。
- 以 `interviewId` 而非 `userId` 为键，可支持同一用户的多场并行面试。

## 2. 前端技术文档版本过期

### 现状

旧技术文档仍标注 Vite 5 和 React Router 6；当前 `package.json` 实际使用 Vite 8 和 React Router 7。

### 影响

- 新成员按旧文档安装或排查问题时，可能得到不兼容的配置建议。
- Vite 8 对 Node.js 的最低版本要求更高，旧文档的 Node.js 18+ 说明不再准确。
- 依赖升级、漏洞修复和构建行为难以追溯。

### 改进想法

- 将技术文档中的依赖版本、Node.js 要求、构建命令和路由说明与 `package.json` 同步。
- 在发布或升级依赖时，将文档更新作为同一个提交的一部分。
- 可在 CI 中增加 `npm run build` 和依赖审计，防止文档与实际运行环境长期偏离。

# **Idea**

* 面试可以加入知识库。1.企业风格 2.面经
* 可以选择面试官，hr面还是技术面之类的
* 面试可以分为练习和模拟正式面试。练习面试可以随时暂停并且用户可以随时记录问题，也可以生成提示帮助用户回答。比如一个个问题回答，直到用户知道这道题怎么答了或者点击跳过再进行下一题回答。正式面试不能暂停，最后再生成反馈给用户。（每次面试不应该共享上下文，只知道招聘要求和用户资料）。可以选择面试的难易程度
* 面试结束应该显示报告正在生成中然后直接返回首页或者哪里，在查看报告的位置显示在生成中就行了。生成面试记录的文本：用户可以对问题和答案进行收藏和编辑
* 开始面试页面修改：1.根据招聘要求自动识别岗位，修改简历，匹配关键词。所以招聘要求可以放在最前面 2.历史记录位置不够明显，我用了几次才发现
* （待开发）可以帮助用户进行语音语气分析
* 爬各个公司的网站显示在网页上/根据简历推荐岗位，一键粘贴岗位要求
* 可以选择面试官风格（比如轻松还是压力面）
* 分析报告方面：1.没有明确的岗位能力评分表
  现在主要让 Gemini 自己推断岗位要求，没有先把 JD 转成“技术能力、沟通能力、项目经验”等明确评分维度。2.简历没有直接进入最终报告 Prompt
  无法系统判断回答是否遗漏了简历中最有价值的经历。3.没有企业面经或真实招聘评价作为依据
  目前的“优秀回答标准”主要来自 Gemini 的通用招聘知识，不是特定企业的真实偏好。4.没有固定证据引用要求
  Prompt 虽然要求不能虚构，但“改进方向”没有强制标出它依据的是哪一句回答、哪条 JD。--也就是需要一个完整的评估系统和证据

## 1. 用真实企业面经构建可扩展的 RAG 知识库

### 目标

将经过授权、脱敏和审核的真实企业面经用于实时面试的题目设计、追问方向和回答评分参考。该方案通过 RAG（检索增强生成）为 Gemini 提供上下文，不直接训练或修改 Gemini 的模型参数；因此内容更新可立即生效，也可追溯和删除。

### 数据治理与结构化

- 面经投稿应先进入审核流程；移除姓名、联系方式、内部项目名、未公开信息及其他敏感或保密内容。
- 原始投稿仅用于审核和追溯，不直接注入 Prompt；审核通过后按“一个问题 / 一个考察点”切分为知识片段。
- 每个片段补齐元数据，例如企业、岗位、年限、轮次、技术标签、问题、考察点、答案摘要、可信度、审核状态和来源。
- 对低质量、过期或存在争议的内容进行降权或归档。

### 检索与生成流程

```text
投稿面经
  → 脱敏、审核、清洗、切分和打标签
  → 预先生成 Embedding 并写入向量索引
  → 实时面试根据岗位、技术栈、企业和当前问题进行检索
  → 取少量高质量片段作为上下文
  → LangGraph 将检索结果写入状态并构造 Prompt
  → Gemini 生成题目、追问或评分反馈
```

在 LangGraph 中可在 `ask_technical`、`analyze_answer` 等节点之前加入 `retrieve_experiences` 节点。该节点把检索结果写入 `state.retrievedContext`，后续节点再将其填入对应 Prompt。

Prompt 应明确：面经仅作为题目方向和评分参考；不得泄露投稿者信息；不得声称其代表企业当前或官方招聘标准；优先基于内容生成新问题，避免照搬原题。

### 大规模知识库方案

知识库较大时，不能将全部面经放入 Prompt，也不应遍历本地 JSON。建议采用“元数据筛选 + 向量检索 + 重排序”的分层方案：

```text
大量面经片段
  → 企业 / 岗位 / 年限 / 技术栈 / 审核状态等元数据预筛选
  → 向量相似度检索，召回 Top 20
  → 重排序
  → 最终取 Top 3～5 个片段注入 Prompt
```

- 初期可直接使用 Supabase PostgreSQL 的 `pgvector` 扩展保存 Embedding 与检索索引。
- 数据量或并发显著增长后，可考虑同步到专用向量数据库，如 Qdrant、Pinecone、Weaviate 或 Milvus。
- Embedding、清洗和切分应在投稿后的异步任务中预计算；实时面试只做查询，不处理原始全文。
- 按 `interviewId + topic` 缓存连续追问的检索结果；检索失败或置信度过低时，降级使用现有通用技术知识库。

### 验证效果

建立脱敏的评测集，持续衡量题目相关性、追问质量、评分一致性、检索命中率和用户满意度。RAG 用于补充可更新的事实与案例；是否“提升模型效果”应以这些评测指标判断，而不是仅凭主观体验。

# **体验建议**

1.传文字的时候中文有些时候会转出来韩语，但是语音识别是对的

2.中文语音还是比较生硬

3.在ai面试的时候用户说的话也可以实时显示出来，而不是说话完才把这个段显示出来（可有可无？）

4.live语音问题：如果用户稍微延迟一下比如额或者停顿ai就会打断，这里可以设置一个ai语音延迟

5.日志反映出几个明显问题：

- 语音被频繁切成两段，例如同一句用户回答分成第 4、6 条。
- AI 的半句话也被保存了，例如“听起来很丰富！你提到”，随后又出现完整问题。
- 中文回答有一段被错误识别成韩文。
- 转写结果存在大量不自然的字间空格。
- 面试没有正常结束，很可能用户提前关闭页面、断开连接，或结束流程没有成功调用，因此状态未变成 completed，报告也没有生成。
